export enum EnumOrderStatus {
	CANCELED = "CANCELED",
	PENDING = "PENDING",
	IN_ROAD = "IN_ROAD",
	DELIVERED = "DELIVERED",
}

export enum EnumTransactionStatus {
	CANCELED = "CANCELED",
	PENDING = "PENDING",
	SUCCEEDED = "SUCCEEDED",
}
